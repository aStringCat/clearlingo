# 隐私说明

清译 ClearLingo 不收集、出售或上传遥测数据，也没有开发者自有服务器。

当且仅当用户点击“翻译当前网页”或使用快捷键时，扩展才会读取正文式文本块。Chrome 的内置 Translator API 可用时，翻译在浏览器管理的本地语言模型中完成；否则文本会直接发送到 `api.mymemory.translated.net` 获取译文。密码框、输入框、可编辑区域、代码块和表单内容不会被读取或发送。

目标语言与显示方式保存在浏览器提供的同步存储中。译文只保存在当前页面 DOM 和扩展后台的短期内存缓存中；关闭页面或后台进程结束后不会持久保留。

使用在线回退服务时，翻译文本受 MyMemory 对应服务条款与隐私政策约束。
